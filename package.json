{
  "name": "process-brief",
  "private": true,
  "type": "module",
  "scripts": { "test": "node --test tests/*.test.js", "check": "wrangler deploy --dry-run --keep-vars" },
  "devDependencies": { "wrangler": "^4.123.0" }
}
